mlx_t *mlx		_init(width of window, height of window, title, bool resize)

mlx_image_t 	*mlx_new_image(window, width, height);

void			mlx_set_instance_depth(mlx_instance_t *instance, zdepth)


void			mlx_delete_image(window, image) //might remove every instace



mlx_texture_t* mlx_load_png(const char* path)

mlx_image_t* mlx_texture_to_image(mlx_t* mlx, mlx_texture_t* texture)


Things to do`

check what happens with bad image path
check every malloc fail (check free etc)
